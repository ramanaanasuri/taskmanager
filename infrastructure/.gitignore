# Terraform state and secrets
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform/
.terraform.lock.hcl
crash.log

# AWS config with real IDs
.aws-config
!.aws-config.example

# GCP config with real IDs
.gcp-config
!.gcp-config.example
